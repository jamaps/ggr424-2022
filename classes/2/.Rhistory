PlantGrowth
PlantGrowth %>%
group_by(group) %>%
# the below line is like numSummary, but within a dyplr group_by
# note that n() provides the number of observations in that group
summarize(mean(weight),sd(weight),IQR(weight),n())
library(tidyverse)
install.packages("tidyverse")
install.packages("httr")
install.packages("curl")
install.packages("curl")
install.packages("httr")
install.packages("rvest")
install.packages("xml2")
install.packages("xml2")
install.packages("rvest")
install.packages("tidyverse")
library(tidyverse)
PlantGrowth
PlantGrowth %>%
group_by(group) %>%
# the below line is like numSummary, but within a dyplr group_by
# note that n() provides the number of observations in that group
summarize(mean(weight),sd(weight),IQR(weight),n())
PlantGrowth %>%
group_by(group) %>%
# the below line is like numSummary, but within a dyplr group_by
# note that n() provides the number of observations in that group
summarize(mean(weight),sd(weight),IQR(weight),n())
PlantGrowth %>%
group_by(group) %>%
# the below line is like numSummary, but within a dyplr group_by
# note that n() provides the number of observations in that group
summarize(mean(weight),sd(weight),IQR(weight),n())
library(dplyr)
PlantGrowth %>%
group_by(group) %>%
# the below line is like numSummary, but within a dyplr group_by
# note that n() provides the number of observations in that group
summarize(mean(weight),sd(weight),IQR(weight),n())
\
library(ISLR)
install.packages(ISLR)
install.packages("ISLR")
library(ISLR)
insdata(package="ISLR")
carseats<-Carseats
require(tree)
install.packages("tree")
High = ifelse(carseats$Sales<=8, "No", "Yes")
carseats = data.frame(carseats, High)
tree.carseats = tree(High~.-Sales, data=carseats)
require(tree)
tree.carseats = tree(High~.-Sales, data=carseats)
summary(tree.carseats)
tree.carseats
View(tree.carseats)
tree.carseats = tree(High~Income, data=carseats)
summary(tree.carseats)
tree.carseats = tree(as.factor(High)~Income, data=carseats)
summary(tree.carseats)
library(ISLR)
insdata(package="ISLR")
carseats<-Carseats
require(tree)
High = as.factor(ifelse(carseats$Sales<=8, 0, 1))
carseats = data.frame(carseats, High)
tree.carseats = tree(High~Income, data=carseats)
tree.carseats = tree(High~Income, data=carseats)
library(ISLR)
R.version
library(ggplot2)
setwd("~/Dropbox/teach/ggr424-2022/classes/2")
df <- read.csv("cars_canada.csv")
df$rate <- df$cars / df$people
df
ggplot() +
geom_point(aes(x = df$year, y = df$cars)) +
geom_point(aes(x = df$year, y = df$people)) +
geom_line(aes(x = df$year, y = df$cars)) +
geom_line(aes(x = df$year, y = df$people)) +
theme_minimal()
ggplot() +
geom_line(aes(x = df$year, y = df$rate))
